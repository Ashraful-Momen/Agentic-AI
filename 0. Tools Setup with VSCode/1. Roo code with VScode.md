1. Install the roo extension on the vscode :
---------------------------------------------
a. click roo code extension icon -> select provider option(left menu) ->  
  - configuration profile : default.
  - Api provider -> z Ai (set the api provider which llm model api key accessable to you)
  - z ai entrypoint -> set the url -> select international
  - set the z ai api key -> *****
  - model -> glm-4.6
  - tik mark -> enable reasoinng.

b. active the remote browser setting by chrome or the firefox . 

c. enable the notification all option 

d. set the context option 

e. set the terminal option -> active all option (use Inline terminal ) 

f. prompts : select the enhence prompts opton

g. browser : add the remote bugging on the browser with the chromimum 
  - terminal : chromium --remote-debugging-port=9222 --user-data-dir=/tmp/chromium-dev
  - select the test btn option .
h. openrouter api key : goto url => https://openrouter.ai => create account and api key then set to the roo code input fields.

i. goto marketplace option (in the top menu beside the setting option ) and select mode => install all option with the global mode . 

j. goto marketplace option (in the top menu beside the setting option ) and select mcp => connect all mcp (MCP: LLM ---> connect stdio ------> our device). 

